# protoc-gen-connection (PGC)
*This project is currently in **alpha**. The API should be considered unstable and likely to change*

PGC is a protoc plugin to generate pre-configured gRPC client and connection

